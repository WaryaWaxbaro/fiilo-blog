<%= form_with(model: [blog_element]) do |form| %>
  <% if blog_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog_element.errors.count, "error") %> prohibited this blog_element from being saved:</h2>

      <ul>
        <% blog_element.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :post_id, value: post.id %>
  <%= form.hidden_field :element_type, value: 'image' %>

  <div class="field">
    <%= form.label :blog_image %>
    <%= form.file_field :blog_image, accept: 'image/*' %>
  </div>

  <div class="field">
    <%= form.label :blog_image_caption %>
    <%= form.text_field :blog_image_caption %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
